import Link from "next/link";
export type AdminTableRow = { cells: React.ReactNode[]; id?: string; action?: React.ReactNode };

export function AdminListing({title,description,headers,rows,pagination}:{title:string;description:string;headers:string[];rows:AdminTableRow[];pagination?:{path:string;query:string;page:number;hasMore:boolean}}) {
 const link=(page:number)=>`${pagination?.path}?q=${encodeURIComponent(pagination?.query??"")}&page=${page}`;
 return <div className="mx-auto max-w-[1500px]"><p className="text-xs font-bold uppercase tracking-[.2em] text-emerald-600">Administration</p><h1 className="mt-1 text-3xl font-bold">{title}</h1><p className="mt-1 text-sm text-slate-500">{description}</p><div className="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">{pagination&&<form method="get" className="flex gap-2 border-b border-slate-100 p-4"><input name="q" defaultValue={pagination.query} placeholder="Search database records" className="min-w-0 flex-1 rounded-xl border border-slate-200 px-3 py-2 text-sm"/><button className="rounded-xl bg-slate-950 px-4 py-2 text-sm font-bold text-white">Search</button></form>}<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr>{headers.map(h=><th key={h} className="whitespace-nowrap px-5 py-3">{h}</th>)}</tr></thead><tbody>{rows.length?rows.map((r,i)=><tr key={r.id??i} className="border-t border-slate-100">{r.cells.map((c,j)=><td key={j} className="whitespace-nowrap px-5 py-3">{c}</td>)}{r.action&&<td className="whitespace-nowrap px-5 py-3">{r.action}</td>}</tr>):<tr><td colSpan={headers.length} className="p-12 text-center text-slate-400">No records found in the database</td></tr>}</tbody></table></div>{pagination&&<div className="flex items-center justify-between border-t border-slate-100 p-4 text-xs"><span>Page {pagination.page}</span><div className="flex gap-2">{pagination.page>1&&<Link href={link(pagination.page-1)} className="rounded-lg border px-3 py-2">Previous</Link>}{pagination.hasMore&&<Link href={link(pagination.page+1)} className="rounded-lg border px-3 py-2">Next</Link>}</div></div>}</div></div>;
}
