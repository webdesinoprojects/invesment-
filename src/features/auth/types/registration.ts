export type RegistrationReceiptData = {
  memberId: string;
  fullName: string;
  email: string;
  joinedAt: string;
};

export type RegistrationSecrets = {
  password: string;
  securityPin: string;
};
